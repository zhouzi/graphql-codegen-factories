enum UserRole {
  SUPER_ADMIN
  ADMIN
}

type User {
  id: ID!
  role: UserRole!
  companion: Companion!
}

type Droid {
  id: ID!
  codeName: String!
}

type Dog {
  id: ID!
  name: String!
}

union Companion = Droid | Dog

type Query {
  users: [User!]!
}

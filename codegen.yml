overwrite: true
schema: ./__tests__/schema.graphql
generates:
  ./__tests__/types.ts:
    plugins:
      - typescript
      - ./build/index.js
    config:
      factoryName: new{Type}
      scalarDefaults:
        Boolean: true
        Date: new Date()
